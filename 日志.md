# 2026-06-10 工作日志

## 今日目标
- 接入 YOLO11 红绿灯检测模型
- 巡线从 PD 升级为 PID 控制
- 避障从开环改闭环（航向 PID）
- 联调整条链路：巡线+避障+红绿灯+终点线

---

## 1. YOLO11 红绿灯模型部署

### 模型部署
- 使用 `imported_package_20260528` 中的 BPU 模型：`yolo11n_detect_bayese_640x640_nv12_modified.bin`
- 模型内部名为 `yolo11n_detect_bayese_640x640_nv12`（配置必须匹配）
- 部署到 `/userdata/yolo11/`：
  - 模型文件 `.bin`
  - `yolov11workconfig.json`（parser=yolov8, 3类, strides=[8,16,32]）
  - `config/traffic_light.list`（green_light / blue_light / red_light）
  - `dnn_node_yolo.launch.py`、`view_traffic.py`

### 控制端集成
- `lanedetection.py` 订阅 `/traffice_sign` (PerceptionTargets)
- 红灯→强制 STOP（不自动恢复），绿灯→恢复 TRACK
- 连续 2 帧确认，防闪烁

---

## 2. 巡线 PD → PID 升级

### 信号链全面优化
- 错误公式：`error = target_x - point_x`（正值左转）
- PID 控制：P + 积分抗饱和(±600) + 微分低通
- 动态速度：弯道 `speed = min + (max-min) * (1 - |err|/target_x)`
- 输出 blend：速度/转向分别低通平滑
- 加速度限制：区分加速/减速方向

### 关键参数演进

| 阶段 | kp | kd | deadband | target_x | max_linear_x | 说明 |
|------|-----|-----|----------|----------|-------------|------|
| 初始 | 0.0015 | 0.0005 | 55 | 960 | 0.22 | 从 sample_code 移植 |
| 震荡修复 | 0.0010 | 0.0010 | 30 | 960 | 0.22 | 增益+死区调整 |
| 滤波快化 | 0.0010 | 0.0010 | 15 | 960 | 0.35 | alpha/blend提到0.4-0.6 |
| 弯道加强 | 0.0013 | 0.0008 | 15 | 960 | 0.45 | kp提，kd降 |
| 贴内道修复 | 0.0018 | 0.0008 | 8 | 1000→940→920→900 | 0.50 | 逐步修正target_x |
| 最终 | 0.0022 | 0.0008 | 8 | 900 | 0.50 | 弯道转向更强 |

### 重要发现
- `target_x` 不是像素中心960，摄像头有物理偏置，实测约900
- 滤波太重（α=0.3）导致相位滞后→追逐式震荡，提到0.6解决
- blend 太低也加剧滞后，从0.24提到0.4
- 死区从55px缩小到8px改善纠偏

---

## 3. 避障链路改造

### 架构重构
- 统一输出 `publish_motion(lx, az)`：所有状态共享 blend + 加速度限制
- 提取 `compute_lane_pid(point_x)`：TRACK/APPROACH/SEARCH 共用
- 新增 `heading_to_angular()`：航向P控制

### 状态机（当前版本）
```
TRACK ──[front < slow_range(0.75m)]──→ APPROACH
  ↑                                       │
  │                              [front < trigger_range(0.50m)]
  │                                       ↓
  │                                 AVOID (plan_avoid → execute_avoid)
  │                                       │
  │                              [ready or timeout 5s]
  │                                       ↓
  │                                 RETURN (execute_return, min 2s)
  │                                       │
  │                              [return 完成 or timeout 10s]
  │                                       ↓
  └────[line reacquired]──────  SEARCH (视觉PID搜线, timeout 8s)
```

### 避障方向问题（未完全解决）
- 用 odom 航向 PID 做 AVOID/RETURN 时方向反了→翻转 arc_direction 映射
- 怀疑 odom yaw 漂移（轮式里程计），导致 RETURN 回正角度不准
- 回正后车头方向不正确 → 巡线时横穿赛道

### 二次触发防护
- RETURN/SEARCH 阶段不再响应障碍物触发（一次避障=一次闭环）
- `obs_cooldown=5.0s`（回 TRACK 后冷却）
- STOP 恢复不再检查障碍物

### 当前避障参数
| 参数 | 值 | 说明 |
|------|-----|------|
| slow_range | 0.75m | 开始减速 |
| trigger_range | 0.50m | 开始转弯 |
| avoid_speed | 0.12 | 绕障线速度 |
| avoid_turn_angle | 1.30rad(~75°) | 绕障目标转角 |
| avoid_yaw_kp | 1.2 | 航向P增益 |
| avoid_timeout | 5.0s | 绕障超时 |
| min_return_time | 2.0s | 最小回转时间 |
| search_timeout | 8.0s | 搜线超时 |
| line_reacquire_error_px | 800 | 搜线判决误差容限 |

### 已知问题
1. odom 航向不可靠导致 RETURN 方向不准→考虑改用固定角速度+时间控制
2. SEARCH 搜线后 TRACK 阶段车头方向可能不准→横穿赛道

---

## 4. 终点线检测
- 远端已有 `finish_detection.py`：棋盘格（黑白格子）检测
- 订阅 `/image`，ROI 裁剪→网格化→对比度/过渡率分析
- 连续2帧确认→发布 `/finish_detected=True`→`publish_stop()`
- 已同步到本地 repo
- **本次未做改动**，全程保留

---

## 5. 启动脚本完善
- 节点联动顺序（8步）：bringup → codec → YOLO → obstacle → perception → visualization → lanedetection → websocket
- `use_lidar:=true`
- obstacle 入口点名为 `obstacle`（不是`obstacle_detection`）
- 停止脚本含所有节点的 pkill

---

## 6. 本地文件同步
从远端同步到本地的文件：
- `lanedetection/lanedetection/finish_detection.py`
- `lanedetection/lanedetection/recovery_logic.py`  
- `lanedetection/lanedetection/obstacle_detection.py`

---

## 当前状态
- 巡线：PID稳定，弯道转向尚可，直线速度0.50m/s
- 避障：状态机完整可运行，但 RETURN 后方向不准→横穿赛道
- 红绿灯：模型已部署，控制端已接入，未实测
- 终点线：已有，未改动

## 下一步
1. 修复 RETURN 方向问题：odom 不可靠，考虑用固定角速度+时间替代航向PID
2. 横穿赛道后如何快速恢复巡线
3. 红绿灯实车验证
4. 如稳定，逐步提速

---

# 2026-05-07 工作日志

## 今日目标
- 让 OriginBot 小车在赛道上稳定巡线
- 打通网页调试画面
- 排查并接通激光雷达
- 根据实验指导 9 接入避障链路
- 尝试把避障逻辑从“停车”升级为“绕行”

## 今天完成的工作

### 1. 巡线感知与网页调试链路
- 重启并整理了以下链路：
  - `originbot_base`
  - `hobot_usb_cam`
  - `hobot_codec`
  - `line_follower_perception`
  - `line_follower_visualization`
  - `websocket`
- 网页端调试画面统一使用：
  - `/image_marked`
  - 地址：`http://10.197.15.170:8000`
- 在可视化画面中叠加了：
  - 巡线点
  - 控制输出 `cmd_vel`
  - 障碍物状态 `has_obs`

### 2. 修复巡线相关问题
- 延续并验证了之前的图像尺寸适配修复，确保感知节点可以稳定输出巡线点
- 在控制节点中补充了调试输出：
  - `/debug/lane_state`
- 多次调整巡线参数：
  - 直线速度
  - 转向增益
  - 转向死区
  - 弯道内切修正
- 最终把巡线速度改到固定线速度版本，便于直接测试速度效果

### 3. 激光雷达问题排查
- 初期雷达无法工作，原因经历了两个阶段：
  1. 系统里根本没有识别出雷达串口设备
  2. 后续识别出设备后，配置又错误占用了底盘串口
- 最终确认：
  - 底盘默认串口是 `ttyS1`
  - 雷达实际通过 CH340 转串口枚举为 `ttyUSB0`
- 新增并生效了 udev 规则：
  - `ttyUSB0 -> /dev/nvilidar`
- 单独启动 VP100 后，已经可以看到：
  - `/scan`

### 4. 雷达参数验证
- 对 VP100 驱动参数做了多组测试：
  - 波特率 `115200`
  - 波特率 `230400`
  - `reversion` / `inverted` 不同组合
- 结果：
  - `115200` 下驱动可起，但扫描数据不稳定
  - `230400` 下可以稳定收到 `/scan`
- 说明当前雷达更适合使用：
  - `serialport_name: /dev/nvilidar`
  - `serialport_baud: 230400`

### 5. 避障节点升级
- `obstacle_detection.py` 已不再只输出简单布尔量
- 当前同时输出：
  - `/has_obs`
  - `/obstacle_info`
  - `/debug/obstacle_state`
- `obstacle_info` 中包含：
  - `front_min_dist`
  - `left_min_dist`
  - `right_min_dist`
  - `recommended_side`
  - `nearest_range`
  - `nearest_angle_deg`

### 6. 巡线控制升级为状态机
- `lanedetection.py` 已从单纯巡线控制，改成 4 状态控制器：
  - `TRACK`
  - `AVOID_DECIDE`
  - `AVOID_BYPASS`
  - `RETURN_TO_LINE`
- 核心思路：
  - 正常巡线时使用视觉点控制
  - 障碍物进入前方扇区后进入决策态
  - 根据左右空闲度选择绕行方向
  - 绕过障碍后再逐步回到赛道线
- 当前代码已经同步到远端并完成编译

## 今日产生的重要文件/改动
- 本地与远端同步修改：
  - `lanedetection/lanedetection/lanedetection.py`
  - `lanedetection/lanedetection/obstacle_detection.py`
  - `line_follower_perception/src/line_follower_visualization.cpp`
  - `line_follower_perception/include/line_follower_perception/line_follower_visualization.h`
  - `line_follower_perception/CMakeLists.txt`
- 新增：
  - `view_lidar.py`
- 远端脚本：
  - `/userdata/codex_stop_all.sh`
  - `/userdata/codex_start_line_only.sh`
  - `/userdata/codex_start_line_obstacle.sh`

## 当前状态
- 小车当前已停止，未保留主动运动链路
- `lanedetection` 最新状态机版已编译通过
- 雷达设备已能识别并可映射到 `/dev/nvilidar`
- `/scan` 在 `230400` 参数下可以收到
- 还没有完成“正式绕障实车验证”

## 下一步建议
1. 把 VP100 默认参数固定到 `230400`
2. 单独启动：
   - 底盘
   - 雷达
   - `obstacle_detection`
   验证 `recommended_side` 是否稳定
3. 再启动 `lanedetection` 状态机版
4. 用低速先验证：
   - 能否进入 `AVOID_DECIDE`
   - 能否绕过障碍
   - 能否从 `RETURN_TO_LINE` 回到赛道
5. 绕障稳定后，再逐步提升速度
